import { m as statusLabel } from "./cinematic-CKTwKQYq.js";
import { jsx } from "react/jsx-runtime";
//#region src/components/status-pill.tsx
function StatusPill({ status }) {
	const colors = {
		pending: {
			bg: "bg-warning/15",
			text: "text-warning"
		},
		review: {
			bg: "bg-blue-500/15",
			text: "text-blue-400"
		},
		approved: {
			bg: "bg-success/15",
			text: "text-success"
		},
		rejected: {
			bg: "bg-destructive/15",
			text: "text-destructive"
		}
	}[status];
	return /* @__PURE__ */ jsx("span", {
		className: `rounded-full px-3 py-1 text-[11px] ${colors.bg} ${colors.text}`,
		children: statusLabel[status]
	});
}
//#endregion
export { StatusPill as t };
